<div class="cta-widget">
  <div class="cta-widget-inner">
    <div class="cta-widget-phone">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
      </svg>
      <a href="tel:+34697711344">697 71 13 44</a>
    </div>

    <div class="cta-widget-text">
      <span class="cta-widget-heading">¿Hablamos?</span>
      <span class="cta-widget-sub">Te asesoramos gratis y sin compromiso</span>
    </div>

    <a href="/contacto/" class="cta-widget-btn">
      Quiero que me llaméis
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
    </a>
  </div>
</div>

<style is:global>
  .cta-widget {
    width: 100vw;
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw !important;
    margin-right: -50vw !important;
    background: linear-gradient(135deg, #F5A623 0%, #E8951A 100%);
    padding: 32px 20px;
  }

  .cta-widget-inner {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    gap: 32px;
    flex-wrap: wrap;
  }

  .cta-widget-phone {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-shrink: 0;
  }

  .cta-widget-phone svg {
    width: 42px;
    height: 42px;
    color: rgba(255,255,255,0.9);
    background: rgba(255,255,255,0.15);
    border-radius: 50%;
    padding: 10px;
    flex-shrink: 0;
  }

  .cta-widget-phone a {
    color: #fff;
    font-size: 24px;
    font-weight: 700;
    text-decoration: none;
    letter-spacing: 0.5px;
    white-space: nowrap;
  }

  .cta-widget-phone a:hover {
    text-decoration: underline;
  }

  .cta-widget-text {
    flex: 1;
    min-width: 200px;
  }

  .cta-widget-heading {
    display: block;
    color: #fff;
    font-size: 22px;
    font-weight: 700;
    line-height: 1.2;
  }

  .cta-widget-sub {
    display: block;
    color: rgba(255,255,255,0.85);
    font-size: 15px;
    margin-top: 2px;
  }

  .cta-widget-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: #fff;
    color: #1a1a1a;
    font-size: 16px;
    font-weight: 700;
    padding: 14px 28px;
    border-radius: 50px;
    text-decoration: none;
    white-space: nowrap;
    flex-shrink: 0;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.15);
  }

  .cta-widget-btn svg {
    width: 18px;
    height: 18px;
    transition: transform 0.3s ease;
  }

  .cta-widget-btn:hover {
    background: #1a1a1a;
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(0,0,0,0.25);
  }

  .cta-widget-btn:hover svg {
    transform: translateX(3px);
  }

  @media (max-width: 768px) {
    .cta-widget-inner {
      flex-direction: column;
      text-align: center;
      gap: 20px;
    }

    .cta-widget-text {
      min-width: unset;
    }

    .cta-widget-phone a {
      font-size: 20px;
    }

    .cta-widget-btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
